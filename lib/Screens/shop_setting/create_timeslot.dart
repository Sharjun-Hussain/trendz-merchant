import 'package:flutter/material.dart';

class CreateTimeslot extends StatelessWidget {
  const CreateTimeslot({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
